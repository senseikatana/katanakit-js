# Variable: useRetry

> **useRetry**: &lt;`T`&gt;(`fn`, `retries`, `delayMs`) => `Promise`&lt;`T`&gt;

Defined in: [src/core/services/utils.service.ts:183](https://github.com/senseikatana/katanakit-js/blob/5e558a8367455e6129bb2647b2f6a540281bfea4/src/core/services/utils.service.ts#L183)

## Type Parameters

### T

`T`

## Parameters

### fn

() => `Promise`&lt;`T`&gt;

### retries?

`number` = `3`

### delayMs?

`number` = `1000`

## Returns

`Promise`&lt;`T`&gt;
