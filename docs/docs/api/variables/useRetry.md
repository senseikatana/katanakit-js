# Variable: useRetry

> **useRetry**: &lt;`T`&gt;(`fn`, `retries`, `delayMs`) => `Promise`&lt;`T`&gt;

Defined in: [src/core/services/utils.service.ts:183](https://github.com/senseikatana/katanakit-js/blob/7b860163683b97d645cfebc80bef622aba3bde3a/src/core/services/utils.service.ts#L183)

## Type Parameters

### T

`T`

## Parameters

### fn

() => `Promise`&lt;`T`&gt;

### retries?

`number` = `3`

### delayMs?

`number` = `1000`

## Returns

`Promise`&lt;`T`&gt;
