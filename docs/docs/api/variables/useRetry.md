# Variable: useRetry

> **useRetry**: &lt;`T`&gt;(`fn`, `retries`, `delayMs`) => `Promise`&lt;`T`&gt;

Defined in: [src/core/services/utils.service.ts:183](https://github.com/senseikatana/katanakit-js/blob/671263249d5f5da09ae01d788489a7d1f8f3c5fc/src/core/services/utils.service.ts#L183)

## Type Parameters

### T

`T`

## Parameters

### fn

() => `Promise`&lt;`T`&gt;

### retries?

`number` = `3`

### delayMs?

`number` = `1000`

## Returns

`Promise`&lt;`T`&gt;
