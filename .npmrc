# npm v12+ blocks dependency lifecycle scripts by default.
# This project uses pnpm, but this config protects against accidental `npm install`.
# No dependency in this project has lifecycle scripts (preinstall/install/postinstall).
# If a future dependency needs one, add it here explicitly: allow-scripts=package-name
allow-scripts=
