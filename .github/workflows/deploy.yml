name: Deploy

on:
 push:
  branches: [main]
  paths:
   - "content/**"
   - "hooks/**"
   - "mkdocs.yml"
   - "typedoc.json"
   - "playground/**"
   - "src/**"
   - "package.json"
   - "bun.lock"
   - ".github/workflows/deploy.yml"
 workflow_dispatch:

permissions:
 contents: read

jobs:
 pages:
  runs-on: ubuntu-latest
  steps:
   - uses: actions/checkout@v4
     with:
      fetch-depth: 0

   - uses: oven-sh/setup-bun@v2

   - uses: actions/setup-node@v4
     with:
      node-version: 22

   - uses: actions/setup-python@v5
     with:
      python-version: "3.12"

   - run: bun install --frozen-lockfile

   - run: python -m pip install mkdocs-material

   - run: bun run build

   - run: mkdocs build --strict

   - run: bun run playground:build

   - name: Deploy docs
     uses: cloudflare/wrangler-action@v3
     with:
      apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      wranglerVersion: "4.124.0"
      command: pages deploy site --project-name katanakit-docs --branch main --commit-dirty=true

   - name: Deploy playground
     uses: cloudflare/wrangler-action@v3
     with:
      apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      wranglerVersion: "4.124.0"
      command: pages deploy playground/dist --project-name katanakit-playground --branch main --commit-dirty=true
