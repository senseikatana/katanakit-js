# Plan: publicar katanakit-js@2.1.4 en npm

## Contexto
- `katanakit-js` NO existe en npm (paquete nuevo, E404). Todo lo publicado está en `katanakit-dev` (hasta 2.1.0).
- El tag git `v2.1.4` apunta al HEAD actual (`26ca30b`), pero su package.json dice 2.3.0 → hay que alinear la versión a 2.1.4.
- Working tree limpio en `dev`. `dist/` está desactualizado (theme.service.ts se modificó tras el último build).

## Pasos

### 1. Alinear versión local
- Cambiar `"version"` en `package.json` de `2.3.0` → `2.1.4` (coincide con el tag git `v2.1.4`).

### 2. Rebuild + verificaciones (pre-publish)
- `rm -rf dist && tsc -p tsconfig.json` (regenera dist con el último cambio de theme.service.ts).
- `biome check ./src` → debe dar limpio.
- `vitest run` → 57/57 tests verdes.
- `tsc --noEmit` → 0 errores.

### 3. Publish
- `npm publish --access public` — es un paquete NUEVO, así que requiere `--access public` la primera vez (publishConfig ya tiene `"access": "public"` como respaldo).
- El `prepublishOnly` ya corre `check && test && build` automáticamente.

### 4. Verificar
- `npm view katanakit-js dist-tags --json` → debe mostrar `{ latest: "2.1.4" }`.
- `npm view katanakit-js versions --json` → `["2.1.4"]`.

### 5. Git (según lo que pidas; no hago push sin confirmación)
- Commit del cambio de versión en package.json (si lo deseas) y opcionalmente alinear/re-crear el tag `v2.1.4` si quieres que apunte al commit final con `2.1.4` real.

## Notas
- Si npm pide OTP/2FA interactivo, te lo indicaré para que autentiques (o ejecutas `npm publish --access public` tú mismo en `~/Proyectos/katanakit`).
- No se toca `katanakit-dev` (queda con su historial 2.0.0–2.1.0).
